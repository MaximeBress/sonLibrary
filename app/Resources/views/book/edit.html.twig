{% extends 'base.html.twig' %}

{% block body %}
    <h1>Modification du livre</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Modifier" class="waves-effect waves-light btn blue"/>
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('book_index') }}" class="waves-effect waves-light btn"><i class="material-icons left">arrow_back</i>Retour à la liste</a>
        </li>
    </ul>
{% endblock %}
